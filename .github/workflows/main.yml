name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run pester tests
      shell: pwsh
      run: |
        Install-Module -Name Pester
        $res = Invoke-Pester -PassThru
        $res.FailedCount -eq 0
